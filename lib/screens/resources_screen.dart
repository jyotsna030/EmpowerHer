import 'package:flutter/material.dart';

class ResourcesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Legal & Medical Resources")),
      body: Center(child: Text("Nearby resources and FAQs will be displayed here.")),
    );
  }
}
